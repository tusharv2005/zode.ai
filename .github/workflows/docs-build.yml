name: docs-build

on:
  push:
    branches: [main]
    paths:
      - "packages/zode-docs/**"
  pull_request:
    paths:
      - "packages/zode-docs/**"
  workflow_dispatch:

jobs:
  build:
    name: Build docs site
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6 # zodecode_change

      - name: Setup Bun
        uses: ./.github/actions/setup-bun

      - name: Build site
        run: bun run --filter @zodecode/zode-docs build
        env:
          NEXT_PUBLIC_POSTHOG_KEY: ${{ secrets.POSTHOG_API_KEY }}

      - name: Run tests
        run: bun run --filter @zodecode/zode-docs test
